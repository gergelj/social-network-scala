# https://www.playframework.com/documentation/latest/Configuration
slick.dbs.default = {
    driver = "slick.driver.MySQLDriver$"
    db.driver = "com.mysql.jdbc.Driver"
    db.url = "jdbc:mysql://localhost:3306/scalatest"
    db.url = ${?JDBC_DATABASE_URL}
    db.user = "dbuser"
    db.password = "root"
}